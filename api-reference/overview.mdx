---
title: 'Overview'
---

The Sophic Engine API is organized around REST. Our API has predictable resource-oriented
URLs, accepts JSON request bodies (except our OAuth2 endpoints which accepts form-encoded
request bodies), returns JSON-encoded responses, and uses standard HTTP response codes,
authentication, and verbs.

